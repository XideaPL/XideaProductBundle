{% extends 'XideaProductBundle::main.html.twig' %}
{% block xidea_product_header_title %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">{{ 'product_action.list.title'|trans }}</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
{% endblock %}
{% block xidea_product_main %}
    <div class="row">
        <div class="col-lg-8">
            <div class="panel panel-default">
                <div class="panel-heading">
                    {{ 'product_action.list.title'|trans }}
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>{{ 'product.name'|trans }}</th>
                                    <th>{{ 'product.created_at'|trans }}</th>
                                    <th>{{ 'product.updated_at'|trans }}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for object in objects %}
                                    <tr>
                                        <td>{{ object.id }}</td>
                                        <td>{{ object.name }}</td>
                                        <td>{{ object.createdAt|date() }}</td>
                                        <td>{{ object.updatedAt|date() }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
    </div>
{% endblock %}
